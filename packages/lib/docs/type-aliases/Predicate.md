[**@kurone-kito/web-toybox**](../README.md)

***

[@kurone-kito/web-toybox](../globals.md) / Predicate

# Type Alias: Predicate\<T, U\>

> **Predicate**\<`T`, `U`\> = [`Mapper`](Mapper.md)\<`T`, `U`\>

Type definition of the function to test each element of the array,
e.g. `Array<T>.filter()`

## Type Parameters

### T

`T`

The type of the array element.

### U

`U` *extends* `boolean` = `boolean`

The type of the test result.
