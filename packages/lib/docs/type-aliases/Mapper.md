[**@kurone-kito/web-toybox**](../README.md)

***

[@kurone-kito/web-toybox](../globals.md) / Mapper

# Type Alias: Mapper()\<T, U\>

> **Mapper**\<`T`, `U`\> = (`value`, `index`, `array`) => `U`

Type definition of the function to convert each element of the array.
e.g. `Array<T>.map()`

## Type Parameters

### T

`T`

The type of the array element.

### U

`U`

The type of the converted element.

## Parameters

### value

`T`

### index

`number`

### array

readonly `T`[]

## Returns

`U`
